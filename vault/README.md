# Vault

This is the agent's long-term memory / knowledge base, read by the filesystem-based
Obsidian MCP server configured in `.mcp.json` (see root `CLAUDE.md` §4).

What goes here:
- Farhan's notes, pitch draft, task list, and demo script (synced from the Obsidian
  mobile app)
- Any reference material the agent should be able to retrieve at run time

This file is a placeholder so the retrieval pipeline can be tested end-to-end before
real notes exist. Delete it once real content has landed.
