# Solved Challenges — Quick Index

One row per captured flag. Full narrative (agent trace, bugs found, root cause) stays in
`practice_runs.md` or `CLAUDE.md`'s session-update log — this file is just the fast-scan index,
not a replacement for either.

| Date | Platform | Challenge | Category | Flag | Technique | Full write-up |
|---|---|---|---|---|---|---|
| 2026-08-05 | picoCTF | IntroToBurp | web (missing-field validation bypass) | `picoCTF{REDACTED}` | Register via CSRF-protected form, then POST to `/dashboard`'s 2FA form with the `otp` field absent entirely (a wrong-named field in its place also works) — server treats a missing field as success instead of rejecting it | `evals/practice_runs.md` → "IntroToBurp". **Agent's first two runs failed** (cookie lost on plain `GET /` calls, endless re-register loop) — root-caused to `fetch_url` being fully stateless. Fixed by adding a `session_id`-based cookie jar to `fetch_url`; re-run then **solved end-to-end by the agent itself**, 6 steps, no human intervention. Confirmed accepted on picoCTF |
| 2026-08-05 | picoCTF | WebDecode | web (encoded data in included file) | `picoCTF{REDACTED}` | Web inspector → found an encoded string in `about.html` (a file included by the page, not the page itself) → decoded via `identify_and_decode` | This session (dashboard, 6 steps, no `web_search` used — verified grounded in a real tool result, not the flag-hallucination pattern below) |
| 2026-08-05 | picoCTF | head-dump | web (Node.js heap-dump leak) | `picoCTF{REDACTED}` | Swagger UI exposed an undocumented `/heapdump` endpoint leaking a full Node heap snapshot; flag was buried past `fetch_url`'s 8 KB cap | This session — **agent's own run got this wrong first** (fabricated a different, plausible-looking flag from memorized training data on a public writeup instead of admitting the response was truncated); real flag found via direct `curl`+`grep` against the live target, bypassing the cap. Root cause fixed same session: `fetch_url` gained a `search_pattern` mode (server-side regex search over up to 20 MB) plus a stronger anti-hallucination system-prompt rule — see `agent/tools/fetch_url.py`, commit `2a1e004` |
| 2026-08-05 | picoCTF | n0s4n1ty | web (file upload → RCE) | `picoCTF{REDACTED}` | Unsanitized PHP upload → webshell → passwordless `sudo cat /root/flag.txt` | This session (dashboard, HITL approve/deny) |
| 2026-08-05 | picoCTF | Cookie Monster Secret Recipe | web (secret stored in a cookie) | `picoCTF{REDACTED}` | POST'd guessed creds to `/login.php`; login was refused, but the response `Set-Cookie`'d a `secret_recipe` value that base64-decoded straight to the flag | `evals/practice_runs.md` → "Cookie Monster Secret Recipe" (also the run where a dead first instance exposed a real flag-fabrication bug in the agent, fixed same session — see write-up) |
| 2026-08-05 | picoCTF | Unminify | web (flag hidden in minified HTML) | `picoCTF{REDACTED}` | Flag sat verbatim in a `class` attribute inside the minified page source, found via a single `fetch_url` GET — no unminify tooling actually needed | `evals/practice_runs.md` → "Unminify" |
| 2026-08-04 | picoCTF | Old Session | web (session mgmt) | `picoCTF{REDACTED}` | `/sessions` endpoint leaked every active session; swapped own cookie for an admin session | `evals/practice_runs.md` → "Old Session", Attempt 2 |
| 2026-08-04 | HackTheBox | Space Explorer (formerly logged as "Cosmic Explorer" — see Naming note below, now resolved) | web (JSON parsing) | `HTB{REDACTED}` | JSON parser key-casing differential: duplicate `action` keys in different cases, gateway and backend disagreed on which one wins | `vault/techniques/web/json-parser-key-casing-differential.md`; `CLAUDE.md` 2026-08-03/04 session (stuck, as "Cosmic Explorer") and 2026-08-04 session (solved, as "Space Explorer") |
| — | HackTheBox | Desires | web (Go/Fiber + Node SSO) | `HTB{REDACTED}` | Session middleware trusted a client-supplied `username` cookie without checking it against the real session; timestamp-seeded session IDs made forgery easy | `evals/practice_runs.md` → "Desires" |
| 2026-08-05 | picoCTF | Bookmarklet | web (repeating-key subtract cipher) | `picoCTF{REDACTED}` | JS bookmarklet decrypts a hardcoded ciphertext with `(char - key + 256) % 256`, key `"picoctf"` | `evals/practice_runs.md` → "Bookmarklet". **Agent's first run fabricated a flag** trying to hand-compute the cipher in free text, and the dashboard displayed it uncritically (separate bug, also fixed — see write-up). Root causes fixed: new `fetch_and_decode_cipher` tool, dashboard `data-flag` verification, and a `requests`-encoding bug (silently corrupted non-ASCII bytes when the server omits a charset). **Re-run then solved end-to-end by the agent itself**, confirming all three fixes |
| 2026-08-05 | picoCTF | Inspect HTML | web (flag hidden in HTML source) | `picoCTF{REDACTED}` | Flag sat in an HTML comment (`<!--picoCTF{...}-->`) in the page source, found via a single `fetch_url` GET | `evals/practice_runs.md` → "Inspect HTML". Clean solve, no bugs hit — 1 tool call, no human intervention |
| 2026-08-05 | picoCTF (local file) | StegoRSA | crypto (RSA private key hidden in image metadata) | `picoCTF{REDACTED}` | Hex-encoded PEM private key hidden in `image.jpg`'s metadata `comment` field; hex-decode it, then RSA-decrypt `flag.enc` with it (PKCS1v15 padding) | `evals/practice_runs.md` → "StegoRSA". **Three real bugs found and fixed in sequence** — (1) worst fabrication yet: agent called zero tools at all, wrote a plausible generic recipe as prose, stated a made-up flag; (2) after adding real RSA tools, the model's only action became a single `search_skills` call (system prompt told it to check skill packs "before relying on general knowledge" unconditionally) and it never touched the actual files; (3) after fixing the priority order, the model correctly tried the directory path from the prompt, got a misleading "no such file" (it's a directory, not a file), concluded the files didn't exist, and fell back to `web_search` — lifting a flag from a public writeup of a *different* instance. Fixed: new `agent/tools/rsa_tools.py` (`extract_hidden_key`/`rsa_decrypt_file`), `read_local_file`, PDF support in `extract_metadata`, a rewritten system-prompt priority order (touch real data before searching reference material), and directory-aware error messages (list contents instead of "no such file") across all four local-file tools. **Re-run then solved end-to-end by the agent itself**, confirming all fixes |
| 2026-08-05 | picoCTF | Insp3ct0r | web (flag split across 3 files, root + 2 assets) | `picoCTF{REDACTED}` | Root page's HTML comment, `mycss.css`'s CSS comment, `myjs.js`'s CSS-style comment each hold a third | `evals/practice_runs.md` → "Insp3ct0r". **Agent got stuck in an unrecoverable loop**: it tried `fetch_and_join_fragments` with `paths=",mycss.css,myjs.js"` (leading empty entry meaning "root page + 2 files"), but the tool silently dropped the empty entry, shrinking 3 paths to 2 with no signal — every retry then only adjusted regex content, never the actual cause. Fixed: an empty `paths` entry now correctly resolves to `base_url` itself instead of being dropped. **Re-run then solved end-to-end by the agent itself**, confirming the fix |
| 2026-08-05 | picoCTF | dont-use-client-side | web (client-side-only password check, flag split across 8 substring checks on one page) | `picoCTF{REDACTED}` | A client-side JS `verify()` checks 8 substrings of a password field; each comparison reveals one eighth of the flag directly in the page source | `evals/practice_runs.md` → "dont-use-client-side". **Agent correctly identified all 8 real fragments** but hand-typing them in its final answer introduced a spurious extra character (`picoCTF{REDACTED}`, wrong) — a third variant of the manual-reassembly bug, this time within one page instead of across files. Fixed: `fetch_and_join_fragments` now caches fetches by URL so repeating one path (once per fragment) works efficiently, and docs updated to point at this pattern explicitly. **Re-run then solved end-to-end by the agent itself**, confirming the fix |
| 2026-08-05 | picoCTF | Get aHead | web (HTTP verb tampering) | `picoCTF{REDACTED}` | GET's response never sends a `flag:` header, but `HEAD`'s response does — neither of the page's two visible GET/POST choices is the real answer | `evals/practice_runs.md` → "Get aHead". **Agent correctly inferred HEAD was the technique** (from the hints) but `fetch_url` hard-blocked every method except GET/POST, rejecting the actual solve attempt before it could reach the target. Fixed by extending `fetch_url` to support the full standard HTTP verb set. **Re-run then solved end-to-end by the agent itself** (2 steps: GET then HEAD, no `web_search`), confirming the fix — same flag value independently corroborated against a separate instance, confirming it's static/non-randomized for this challenge |
| 2026-08-05 | picoCTF | Scavenger Hunt | web (flag split across 5 differently-formatted files) | `picoCTF{REDACTED}` | 5 files (`index.html`, `mycss.css`, `robots.txt`, `.htaccess`, `.DS_Store`), each wrapping its fifth of the flag differently (HTML comment, CSS comment, two `#` comments, plain text); joined via `fetch_and_join_fragments`'s new per-path `patterns` mode | `evals/practice_runs.md` → "Scavenger Hunt". Two distinct bugs found and fixed in sequence: (1) the tool computed the correct flag internally but its debug listing, printed before the "Joined:" line, tricked the flag-detection regex — fixed by reordering the output; (2) a single shared `pattern` couldn't correctly bound `index.html`'s HTML comment (which has no `}` at all), causing the match to run into the next file's content — fixed by adding a `patterns` (per-path) mode. Both verified directly against the live target, then **re-run and solved end-to-end by the agent itself**, confirming both fixes |
| 2026-08-05 | picoCTF | Where are the robots | web (robots.txt disclosure) | `picoCTF{REDACTED}` | `/robots.txt` disallows a specific page (`cc6b1.html`); fetching it directly returns the flag in a `<flag>` tag | `evals/practice_runs.md` → "Where are the robots". Clean solve, no bugs hit, 2 steps |
| 2026-08-05 | picoCTF | logon | web (client-supplied cookie trusted without validation) | `picoCTF{REDACTED}` | Login form is a red herring; setting `Cookie: admin=True` directly on `/flag` returns the flag — the server trusts a client-set admin flag with no server-side check | `evals/practice_runs.md` → "logon". Clean solve, no bugs hit |
| 2026-08-05 | picoCTF | hashcrack | crypto (weak-password hash cracking, 3-round netcat) | `picoCTF{REDACTED}` | Netcat service hands MD5 → SHA-1 → SHA-256 hashes in sequence, each round advanced by sending back the real password; all 3 are common/weak passwords crackable by brute-forcing a wordlist | `evals/practice_runs.md` → "hashcrack". **No tool existed to crack a hash at all** — agent read the banner and stopped. Added `crack_hash` (new `agent/tools/crack_hash.py`, auto-detects algorithm from hash length, built-in common-password wordlist + `wordlist_path` for a custom one). Cracked rounds 1-2 immediately; round 3 (`qwerty098`) missed the initial wordlist — agent correctly used `web_search` as a reference lookup (not fabrication) to find and verify it, then the wordlist was expanded (full 3-digit numeric suffixes for keyboard-pattern base words) so it's covered directly next time. **Re-run then solved end-to-end by the agent itself**, 11 steps, no `web_search` needed, including a clean self-recovery from a transient TCP timeout mid-run |
| 2026-08-05 | picoCTF (local file) | Shared Secrets | crypto (Diffie-Hellman, leaked private exponent) | `picoCTF{REDACTED}` | `message (1).txt` has real `g`, `p`, `A` (public) and a leaked `b` (meant to be secret); `shared = pow(A, b, p)`, then XOR each ciphertext byte with `shared % 256` | `evals/practice_runs.md` → "Shared Secrets". **Two distinct bugs found and fixed in sequence** — (1) `observe()`'s flag regex false-matched the literal placeholder `picoCTF{...}` sitting in the challenge's own generator script (`encryption.py`), ending the run after one tool call before the real data file was ever found — fixed by rejecting placeholder-only matches (`_looks_like_placeholder`); (2) with that fixed, the model correctly derived the whole algorithm but fabricated a wrong flag anyway, having written (never executed) Python narrating the computation — the same "no way to actually run the math" gap as StegoRSA's RSA decryption, fixed with new `agent/tools/math_tools.py` (`modpow`, `dh_shared_secret_decrypt`). **Re-run then solved end-to-end by the agent itself** (4 steps), confirming both fixes |
| 2026-08-05 | picoCTF | Cookies | web (cookie value brute-force / out-of-bounds index) | `picoCTF{REDACTED}` | `name` cookie indexes into a server-side list of cookie types; incrementing it past the normal range (`name=0` → tried values up to `name=18`) returned the flag directly on `/check` | `evals/practice_runs.md` → "Cookies". Clean solve, no bugs hit |
| 2026-08-05 | picoCTF | Includes | web (flag split across two included files) | `picoCTF{REDACTED}` | `style.css` and `script.js` each hold half the flag in a comment (`1of2`/`2of2`); concatenate the two fragments directly | `evals/practice_runs.md` → "Includes". **Agent's first two runs both read both fragments correctly** but introduced a stray space joining them by hand — recurred even after a prompt-only "concatenate precisely" guardrail was added, proving prompting alone wasn't enough (second confirmed case of this, after the header-mangling bug). Fixed at the code level instead: new `fetch_and_join_fragments` tool (`agent/tools/fetch_fragments.py`) extracts and joins fragments server-side. **Re-run then solved end-to-end by the agent itself**, confirming the fix |
| 2026-08-05 | picoCTF | Local Authority | web (client-side-only auth, static hidden hash) | `picoCTF{REDACTED}` | Auth check runs entirely in client-side JS; on "success" it POSTs a **hardcoded** hash (visible in the page source) to `/admin.php` — skip login entirely and POST that hash directly | `evals/practice_runs.md` → "Local Authority". **Agent's first run failed**: it found this exact technique on its second real attempt, but every POST in that run silently failed (header key sent as `contentType` instead of `Content-Type`, so PHP's `$_POST` never populated, no error surfaced). Root cause fixed: `agent/tools/_header_repair.py`, a shared header-key canonicalizer wired into `fetch_url`/`upload_file`. **Re-run then solved end-to-end by the agent itself**, confirming the fix |
| 2026-08-05 | picoCTF | Bypass Me | reverse engineering (setuid ELF, XOR-encoded password) | `picoCTF{REDACTED}` | `radare2_analyze`/`ssh_analyze_binary` (info→symbols→disasm) found `decode_password` XORing an 11-byte constant with `0xAA`; decoded programmatically (first by hand-testing directly, then by the agent itself via a remote `python3 -c` one-liner over SSH), submitted via `ssh_run` | `evals/practice_runs.md` → "Bypass Me". First real-world test of `radare2_analyze` — direct call only at first (found and fixed two real bugs: debug-info binaries need the hex `vaddr`, not the plain symbol name, for `disasm`; 0-byte input silently produced no error), then closed the SSH gap that blocked autonomy (`agent/tools/ssh_session.py`: `ssh_analyze_binary`, `ssh_run`) and **re-ran through the actual agent on a fresh instance — solved end-to-end, no hand-holding**, 12/15 steps, including a real self-recovered wrong turn (`pexpect` unavailable, a typo'd `subproceess` in its own generated code) |
| 2026-08-06 | picoCTF | Even RSA Can Be Broken | cryptography (RSA, even modulus) | `picoCTF{REDACTED}` | `N` is even → `p = 2`, `q = N//2` (verified prime), `phi = N//2 - 1`, `d = e^-1 mod phi`, `m = c^d mod N` → ASCII. Params regenerate per connection, so the script re-fetches them | `evals/practice_runs.md` → "Even RSA Can Be Broken". Script-solved first (the agent's run stalled looping `tcp_open`, and with no tool for textbook RSA from bare integers it could only stall or fabricate — it did fabricate from a `web_search` writeup on a re-run). Fixed by building `agent/tools/rsa_int_tools.py`'s `rsa_decrypt_ints` and fixing three real `tcp_session` bugs (`tcp_open` never read the connect banner; `tcp_send` discarded already-received data when the send failed; the recv loop always burned the full timeout, racing interactive services). **Then solved autonomously by the agent in 4 steps on a fresh instance** — `tcp_open` → `tcp_close` → `rsa_decrypt_ints`, with freshly generated `N`/`c`, so a real computation rather than a recalled answer |
| 2026-08-06 | Hackathon organizer (UCSI26) | Saturn Exchange | web (business-logic race condition / TOCTOU) | `UCSI26{REDACTED}` | `POST /api/withdraw` validates `amount <= balance` per-request with no accounting for other pending withdrawals in the same session; fired 40 concurrent 0.9999 BTC withdrawals against one shared `connect.sid` session cookie, driving the account to a real negative balance at settlement, which tripped a buggy fraud "risk-override" path that leaks the flag directly in the API response instead of just blocking it | `evals/practice_runs.md` → "Saturn Exchange". **Solved by hand via curl, not by the agent** — the dashboard run only did sequential recon (`fetch_url` x2, `dir_enum` x1, found nothing) then asked the human for the flag directly; never attempted concurrency. First false start: racing without a shared session cookie hit 40+ separate fresh accounts, not one shared balance — Express's `connect.sid` mints a new session per cookie-less request. Real gap identified, not yet fixed: no tool can fire multiple concurrent requests sharing one session in a single call, and nothing nudges the model toward "race condition" for challenges describing batched/async settlement |
| 2026-08-06 | Hackathon organizer (UCSI26) | StaffDesk | web (GraphQL IDOR → admin account takeover) | `UCSI26{REDACTED}` | Unauthenticated introspection on `/graphql` revealed `user(id)` (any authenticated user, no ownership/role check) and a `User.resetToken` field; leaked admin's (id 1) `resetToken` via `user(id:1)`, then chained straight into `resetPassword(resetToken, newPassword)` before its ~13s rotation window elapsed, took over the admin account, and read `query { flag }` | `evals/practice_runs.md` → "StaffDesk". **Solved by hand via curl, agent not yet tried.** First attempt at `resetPassword` failed (`"invalid reset token"`) purely from latency between two separate manual curl calls crossing the token's rotation boundary — fixed by chaining fetch→reset back-to-back. Real gap identified, not yet fixed: no tool/prompt nudge toward "try GraphQL introspection first" for a `/graphql` target the way `dir_enum` is the reflex for unknown REST routes |
| 2026-08-06 | Hackathon organizer (UCSI26) | Sandworm VM | binary exploitation (custom VM, unchecked-offset OOB read/write) | `UCSI26{REDACTED}` | Custom 8-byte-instruction bytecode VM's `LOAD`/`STORE` opcodes bounds-check the register-held memory index but never re-check after adding the instruction's raw immediate offset — OOB-read a `hook_default` function pointer stored in the VM's state buffer (leaking the PIE base), computed `emit_flag`'s real address via the fixed static delta between the two symbols, OOB-wrote it back over the pointer, then triggered the VM's `HOOK` opcode to call it — `emit_flag` is a real, never-normally-reachable function in the binary that reads the flag file and writes it straight to the socket | `evals/practice_runs.md` → "Sandworm VM"; general technique documented in `vault/techniques/pwn/custom-vm-oob-index-write.md`. **Solved by Claude directly via WSL-bridged radare2 + a standalone Python exploit script, not through the agent loop** — full ISA reconstructed by hand from `main`'s disassembly (not stripped), one exploit built and fired in a single `nc` round-trip. Not yet re-attempted through `agent.graph` — this class of challenge (full custom-ISA reversing + hand-written exploit bytecode) doesn't map cleanly onto any existing tool's designed workflow yet |
| 2026-08-06 | Hackathon organizer (UCSI26) | Pony Express Dispatch | web (SSTI → CVE-2026-33937 Handlebars AST injection RCE) | `UCSI26{REDACTED}` | Classic Handlebars prototype-chain SSTI gadgets are fully blocked (current `allowProtoPropertiesByDefault`/`allowProtoMethodsByDefault` hardening, verified exhaustively — 40+ inherited methods tested, all blocked). Real bug: `Handlebars.compile()` accepts a pre-parsed AST object as well as a string and skips parsing for the former (CVE-2026-33937, Handlebars ≤4.7.8); sending a JSON **object** instead of a string for `template` reaches this path, and a crafted `NumberLiteral.value` string is emitted unsanitized into the compiled JS. Built `{{lookup A B}}` with `A` = an injected IIFE running `process.getBuiltinModule('child_process').execSync(...)` and returning `{result: ...}`, `B` = `"result"`, so `lookup`'s own safe own-property read surfaces the injected output. `/flag` was a directory; read `/flag/flag.txt` | `evals/practice_runs.md` → "Pony Express Dispatch". **Solved by hand via curl, agent not yet tried.** Public PoC payload didn't work verbatim (its brace-closing trick is codegen-version-specific); needed a web search mid-investigation to even find the CVE, then real iteration to get a working injection shape. Real gaps identified, not yet fixed: `fetch_url` has no way to send a nested JSON object (vs. string) for a body field, and nothing nudges the model to search for engine-specific recent CVEs once standard SSTI gadgets fail |

## Not yet solved (for reference, not a full log — see source docs for detail)

| Platform | Challenge | Status | Detail |
|---|---|---|---|
| HackTheBox | SecNotes | Stuck — confirmed airtight | `CLAUDE.md`, 2026-08-03/04 session |
| HackTheBox | Offlinea | In progress, source obtained | `evals/practice_runs.md` → "Offlinea" |
| Hackathon organizer (UCSI26) | EGG V3 (Web, 500 pts, 0 solves) | Stuck — exhausted black-box probing, organizer confirms box is healthy | `evals/practice_runs.md` → "EGG V3". Every method/path/header/cookie/WebSocket/smuggling variant returns an identical empty `200`; only port 3012 is open at all. No public writeup, no earlier version, no attachment/hint. Needs new information (organizer hint or an adjacent challenge's finding), not more fuzzing |

## Naming note (resolved)

"Space Explorer" and "Cosmic Explorer" are the **same HTB target**, not two separate boxes —
confirmed, not just suspected: both `CLAUDE.md` session write-ups describe the identical
`POST /execute` endpoint taking the identical `{"action": "getcosmic"}` / `{"action":
"getSecureCode"}` bodies, and the captured flag (`HTB{REDACTED}`) itself contains "COSMIC."
The 2026-08-03/04 session ("Cosmic Explorer") tried 5 hypotheses and got stuck because none of
them was the actual bug — duplicate/differently-cased JSON keys was never among them. The
2026-08-04 session ("Space Explorer") found that exact technique and captured the flag. Removed
the stale duplicate "stuck" row from the table above accordingly; this is one solved challenge; a
prior write-up name inconsistency, not two challenges.
